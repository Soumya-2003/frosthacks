import { Poppins, Rubik } from "next/font/google";

export const poppins = Poppins({
    weight: '600',
    subsets: ['latin']
})

export const rubik = Rubik({
    weight: "800",
    subsets: ['latin']
})