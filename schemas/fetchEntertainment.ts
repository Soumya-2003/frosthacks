interface SpotifyTrack {
    name: string;
    artist: string;
    image: string;
    
}

interface Movie {
    title: string;
    year: number;
}

interface Game {
    name: string;
    platforms: string[];
}
